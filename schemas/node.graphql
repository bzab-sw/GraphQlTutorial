enum NodeStatus {
  Unknown
  Up
  Down
}

type Node {
  id: ID!
  name: String!
  ip: String!
  configTypes: [String!]!
  comments: String
  status: NodeStatus!
  configs: [Config!]!
}

input NodeInput {
  id: ID!
  name: String!
  ip: String!
  configTypes: [String!]!
}